type Query {
    version: String!
    products: [Product!]!
}

input ProductInput {
    title: String!
    price: Float!
}

type Mutation {
    createProduct(product: ProductInput!): Product!
}

type Product {
    id: Int!
    title: String!
    price: Float!
}