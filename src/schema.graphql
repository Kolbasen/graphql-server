type Query {
    version: String!
    products: [Product!]!
}

input ProductInput {
    title: String!
    price: Float!
}

type Mutation {
    createProduct(product: ProductInput!): Product!
}

type Subscription {
    newProduct: Product
}

type Product {
    id: Int!
    title: String!
    price: Float!
}